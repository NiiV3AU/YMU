[project]
name = "ymu"
version = "1.1.11"
description = "The smart launcher for YimMenu: Always updated, always ready."
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["pyinjector>=1.3.0", "pyside6>=6.11.2", "requests>=2.34.2"]

[dependency-groups]
dev = ["nuitka>=4.2.1", "ruff>=0.16.6"]
